/** Amount step CTA for send flows (balance payout and TLC local pay-in). */
export const SEND_AMOUNT_CONTINUE_CTA = 'Continue'

/** Review step CTA before PIN for balance payout / wallet send. */
export const SEND_REVIEW_CONFIRM_CTA = 'Confirm & Send'

/** Review step CTA for TLC cross-border before pay-in complete. */
export const SEND_REVIEW_CONTINUE_CTA = 'Continue'
