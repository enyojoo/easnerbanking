/** Expo web must use the native RN flag (expo-image); shared default is HTML/Tailwind for Next.js. */
export { CountryFlag } from '@easner/shared/src/components/CountryFlag.native'
export type { CountryFlagProps } from '@easner/shared/src/components/CountryFlag.native'
