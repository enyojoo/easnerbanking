# Example components (demos)

Components in this folder are **reference implementations** and demos used to explore layouts and interactions. They are **not** guaranteed to match production screens pixel-for-pixel.

For product features, prefer shared primitives under `src/components/premium/`, `src/components/ui/`, and theme tokens from `src/theme/`.
