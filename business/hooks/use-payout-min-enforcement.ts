"use client"

/**
 * Re-export for Next — keep client hook out of `@easner/shared` barrel (server routes import shared).
 */
export { usePayoutMinEnforcement } from "../../packages/shared/src/hooks/use-payout-min-enforcement"
