/** Idle duration before soft-lock (has PIN) or sign-out (no PIN). Minutes. */
export const APP_IDLE_TIMEOUT_MINUTES = 5

/** How often to evaluate idle vs last activity (ms). */
export const APP_IDLE_CHECK_INTERVAL_MS = 60_000
