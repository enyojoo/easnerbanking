/** CommandList: height follows item count; scrolls when content exceeds viewport. */
export const DYNAMIC_COMBOBOX_LIST_CLASS =
  "max-h-[min(320px,var(--radix-popover-content-available-height,80vh))] overflow-y-auto overscroll-contain"
