# API Vercel project. Secrets and treasury stay here.
# Host URLs / surface / office origin / store links / Noah return URLs default in code.
# See business/docs/platform-api-split.md → “Api Vercel cleanup”.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
JWT_SECRET=
CRON_SECRET=
EASNER_INTERNAL_CRON_SECRET=

# Optional. API signs app-session with SUPABASE_SERVICE_ROLE_KEY if unset.
# BUSINESS_APP_SESSION_SECRET=
