---
title: Destinations
description: Payment methods for a send — bank, mobile money, wallet, or Easetag.
icon: map-pin
---

A destination is a **payment method**. It is not an account. Accounts (vaults) hold funds. Destinations receive them.

See [Send](/send) for when to use each method.

<Note>
  **Scope:** `GET` needs `accounts.read`. `POST` needs `transfers.write`.
</Note>

## Types

| `type` | Payment method |
|---|---|
| `bank` | ACH, SEPA, Faster Payments, or local bank credit |
| `mobile_money` | Mobile-money wallet |
| `wallet` | On-chain stablecoin address |
| `easetag` | In-network Easner tag |

## Object

| Field | Type | Notes |
|---|---|---|
| `id` | string | `dest_…` |
| `type` | string | One of the types above |
| `customer` | string \| null | `cus_…` if attached |
| `details` | object | Method-specific. Do not log full account numbers |
| `livemode` | boolean | |
| `created` | ISO-8601 | |

## Create

```bash
curl https://api.easner.com/v1/destinations \
  -H "Authorization: Bearer $EASNER_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{"type":"easetag","customer":"cus_…","details":{"easetag":"ada"}}'
```

```json
{
  "id": "dest_…",
  "type": "easetag",
  "customer": "cus_…",
  "details": { "easetag": "ada" },
  "livemode": false,
  "created": "2026-09-20T00:00:00.000Z"
}
```

`201`. `400` `invalid_type` if `type` is unknown.

## Details by type

<AccordionGroup>
  <Accordion title="Bank" icon="landmark">
    **US / ACH**

    ```json
    {
      "type": "bank",
      "details": {
        "account_number": "…",
        "routing_number": "…",
        "bank_name": "…",
        "country": "US",
        "checking_or_savings": "checking"
      }
    }
    ```

    **EUR / SEPA**

    ```json
    {
      "type": "bank",
      "details": {
        "iban": "…",
        "bic": "…",
        "account_holder": "…",
        "bank_name": "…",
        "country": "DE"
      }
    }
    ```

    Also used when the corridor needs them: `sort_code`, `swift_bic`.
  </Accordion>
  <Accordion title="Mobile money" icon="smartphone">
    ```json
    {
      "type": "mobile_money",
      "details": {
        "phone": "+2547…",
        "network": "mpesa",
        "country": "KE"
      }
    }
    ```

    `network` is the operator. `country` is ISO-2.
  </Accordion>
  <Accordion title="Wallet" icon="globe">
    ```json
    {
      "type": "wallet",
      "details": {
        "address": "…",
        "network": "solana",
        "currency": "USDC"
      }
    }
    ```

    | Asset | Networks |
    |---|---|
    | `USDC` | `solana`, `ethereum`, `base` |
    | `USDT` | `tron`, `ethereum`, `solana` |
    | `EURC` | `solana` |

    Live wallet destinations can be priced on the quote. Test stays 1:1.
  </Accordion>
  <Accordion title="Easetag" icon="at-sign">
    ```json
    { "type": "easetag", "details": { "easetag": "ada" } }
    ```

    In-network. Pays a first-party `@tag` or another customer `easetag`. See [Receive](/receive#easetag).
  </Accordion>
</AccordionGroup>

## List

`GET /v1/destinations` → `{ "data": [ Destination, … ] }`

Newest first. Up to 100. There is no next page. There is no `GET` by id — store the id from create. Creating twice creates two destinations (`Idempotency-Key` is not applied here).

## Expected

- One destination per payout rail you support.
- Pass `destination` on the quote. Do not send internal ids (`recipientId`, form sessions). Those are not public.

<Card title="Quotes" icon="tag" href="/quotes">
  Lock send and receive. `source` is required.
</Card>
