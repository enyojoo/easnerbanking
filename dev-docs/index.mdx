---
title: Easner Dev Platform
description: Two products on one key — Banking for your customers, Checkout for payers on your site.
icon: book-open
---

You integrate. Your customer is the banking end user (`cus_`). A payer is someone who pays you through Checkout.

Base URL: `https://api.easner.com`

Auth: `Authorization: Bearer easner_sk_test_…` or `easner_sk_live_…`

Amounts are minor units (cents). Errors are `{ error: { type, code, message } }`. Writes that move money take `Idempotency-Key`.

## Two products

| Product | Who holds the money | What you build |
|---|---|---|
| **Banking** | Your customer, on an issued `acct_` | Accounts, receive, send |
| **Checkout** | You, on your business balance | Collect on your website |

They share a secret key and Console. They do not share a balance.

Checkout uses the same Connect account as invoices and payment links. A successful collect lands on **your** business balance. Switch to Banking in the app to see it. Checkout does not credit a customer `acct_`. That is Receive.

## Objects

| Object | Id | Product | Job |
|---|---|---|---|
| Customer | `cus_` | Banking | Your end user |
| Account | `acct_` | Banking | A currency balance. Issued accounts have `customer` |
| Destination | `dest_` | Banking | Where a send can land |
| Quote | `qt_` | Banking | Locked send / receive amounts |
| Transfer | `tr_` | Banking | Money leaving a customer account |
| Transaction | `txn_` | Both | A row on `GET /v1/transactions` |
| Checkout session | `cs_` | Checkout | A collect on your website |

## Expected flow

<Steps>
  <Step title="Open Dev">
    Switch to Dev on `business.easner.com`.
  </Step>
  <Step title="Mint a test key">
    Open Console. Mint a test key. Add a webhook URL and tick events.
  </Step>
  <Step title="Send (Banking)">
    Create a customer (this issues USD). Add a destination. Quote with `source`. Transfer.
  </Step>
  <Step title="Receive (Banking)">
    Start hosted verification. In test, that approves immediately. Then fetch bank details, chain addresses, or an onramp session onto the customer `acct_`.
  </Step>
  <Step title="Collect (Checkout)">
    Register a website, create sessions from your server, mount `checkout.js`, fulfil from `checkout.completed`.
  </Step>
  <Step title="Go live">
    Mint a live key only after test works. Live receive needs an approved customer.
  </Step>
</Steps>

<Columns cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    First send in test: customer → destination → quote → transfer.
  </Card>
  <Card title="Receive money" icon="download" href="/receive">
    Verification, bank details, chain addresses, onramp.
  </Card>
  <Card title="Checkout" icon="credit-card" href="/checkout">
    Collect onto your business balance.
  </Card>
  <Card title="Authentication" icon="key" href="/authentication">
    Bearer keys, livemode, scopes, idempotency.
  </Card>
</Columns>
