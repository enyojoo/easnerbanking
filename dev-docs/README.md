# Easner Docs

Mintlify project for the Easner API: stablecoin-native banking and payment infrastructure (accounts, receive, send) and Checkout.

```bash
npm i -g mint
mint dev
```

Preview at `http://localhost:3000`. Connect this folder as the docs repo in the [Mintlify dashboard](https://dashboard.mintlify.com) to deploy.
