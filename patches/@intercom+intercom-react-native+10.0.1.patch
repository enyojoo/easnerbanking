diff --git a/node_modules/@intercom/intercom-react-native/lib/commonjs/nativeModules.js b/node_modules/@intercom/intercom-react-native/lib/commonjs/nativeModules.js
index d81f594..d8ad872 100644
--- a/node_modules/@intercom/intercom-react-native/lib/commonjs/nativeModules.js
+++ b/node_modules/@intercom/intercom-react-native/lib/commonjs/nativeModules.js
@@ -12,6 +12,28 @@ function isTurboModuleAvailable(name) {
     return false;
   }
 }
-const IntercomModule = exports.IntercomModule = isTurboModuleAvailable('IntercomModule') ? require('./NativeIntercomSpec').default : _reactNative.NativeModules.IntercomModule;
-const IntercomEventEmitter = exports.IntercomEventEmitter = _reactNative.NativeModules.IntercomEventEmitter;
+const resolvedNativeModule = isTurboModuleAvailable('IntercomModule')
+  ? require('./NativeIntercomSpec').default
+  : _reactNative.NativeModules.IntercomModule;
+const INTERCOM_NOT_LINKED_MSG =
+  'Intercom native module is not linked. Build with the Intercom Expo plugin (expo prebuild / EAS build). Expo Go is not supported.';
+function intercomStub() {
+  return Promise.reject(new Error(INTERCOM_NOT_LINKED_MSG));
+}
+const IntercomModuleFallback = new Proxy(
+  {},
+  {
+    get() {
+      return intercomStub;
+    },
+  },
+);
+const IntercomModule = exports.IntercomModule = resolvedNativeModule ?? IntercomModuleFallback;
+const FALLBACK_EVENT_NAMES = {
+  UNREAD_COUNT_CHANGE_NOTIFICATION: 'IntercomUnreadCountDidChange',
+  WINDOW_DID_HIDE_NOTIFICATION: 'IntercomWindowDidHide',
+  WINDOW_DID_SHOW_NOTIFICATION: 'IntercomWindowDidShow',
+};
+const IntercomEventEmitter = exports.IntercomEventEmitter =
+  _reactNative.NativeModules.IntercomEventEmitter ?? FALLBACK_EVENT_NAMES;
 //# sourceMappingURL=nativeModules.js.map
\ No newline at end of file
diff --git a/node_modules/@intercom/intercom-react-native/lib/module/nativeModules.js b/node_modules/@intercom/intercom-react-native/lib/module/nativeModules.js
index 20e15bc..2d946dd 100644
--- a/node_modules/@intercom/intercom-react-native/lib/module/nativeModules.js
+++ b/node_modules/@intercom/intercom-react-native/lib/module/nativeModules.js
@@ -8,6 +8,29 @@ function isTurboModuleAvailable(name) {
     return false;
   }
 }
-export const IntercomModule = isTurboModuleAvailable('IntercomModule') ? require('./NativeIntercomSpec').default : NativeModules.IntercomModule;
-export const IntercomEventEmitter = NativeModules.IntercomEventEmitter;
+const resolvedNativeModule = isTurboModuleAvailable('IntercomModule')
+  ? require('./NativeIntercomSpec').default
+  : NativeModules.IntercomModule;
+const INTERCOM_NOT_LINKED_MSG =
+  'Intercom native module is not linked. Build with the Intercom Expo plugin (expo prebuild / EAS build). Expo Go is not supported.';
+function intercomStub() {
+  return Promise.reject(new Error(INTERCOM_NOT_LINKED_MSG));
+}
+/** When native Intercom is absent (Expo Go / dev client without rebuild), avoid crashing at import time. */
+const IntercomModuleFallback = new Proxy(
+  {},
+  {
+    get() {
+      return intercomStub;
+    },
+  },
+);
+export const IntercomModule = resolvedNativeModule ?? IntercomModuleFallback;
+const FALLBACK_EVENT_NAMES = {
+  UNREAD_COUNT_CHANGE_NOTIFICATION: 'IntercomUnreadCountDidChange',
+  WINDOW_DID_HIDE_NOTIFICATION: 'IntercomWindowDidHide',
+  WINDOW_DID_SHOW_NOTIFICATION: 'IntercomWindowDidShow',
+};
+export const IntercomEventEmitter =
+  NativeModules.IntercomEventEmitter ?? FALLBACK_EVENT_NAMES;
 //# sourceMappingURL=nativeModules.js.map
\ No newline at end of file
